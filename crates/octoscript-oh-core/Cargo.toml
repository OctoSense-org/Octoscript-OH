[package]
name = "octoscript-oh-core"
version = "0.1.0"
edition = "2021"
description = "The tool registry a Octoscript-OH plugin registers into"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1.0.151"
